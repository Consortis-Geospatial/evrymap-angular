/* eslint-disable @typescript-eslint/naming-convention */
// Capabilities interface for version 1.3.0

export interface ICapabilities {
  WMS_Capabilities: {
    Capability: {
      Layer: {
        Layer: any;
      };
    };
  };
}
